<%= simple_form_for(@wine) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :varietal %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
